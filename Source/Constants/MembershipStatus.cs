namespace Ctma;

static class MembershipStatus
{
    internal const string InReview = nameof(InReview);
    internal const string PendingPayment = nameof(PendingPayment);
    internal const string Approved = nameof(Approved);
}